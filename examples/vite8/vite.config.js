import { defineConfig } from 'vite';

// No special Sass configuration is needed: the point of this example is that a
// stock Vite 8 setup resolves `@use 'sass-mq'` purely from package.json
// `exports`. Vite 8 uses the modern Sass compiler by default.
export default defineConfig({});
